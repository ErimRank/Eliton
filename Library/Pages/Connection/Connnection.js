import { DISPLAYLOADER } from "../DataBase/DisplayLoader/DisplayLoader.js";

export const PAGESCONNECTION=()=>{
const DATA=`const DISPLAYLOADER=${DISPLAYLOADER};
`;
localStorage.setItem("PAGES",DATA);
};
`
const DISPLAYLOADER=${DISPLAYLOADER};
`